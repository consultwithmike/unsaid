import { SeoStub } from "@/components/seo/SeoStub";

export const metadata = { title: "Faith and marriage — what to clarify early — Unsaid" };

export default function Page() {
  return <SeoStub h1="Shared belief, practice, and raising children" />;
}
