import { SeoStub } from "@/components/seo/SeoStub";

export const metadata = { title: "Premarital compatibility — without a fake score — Unsaid" };

export default function Page() {
  return <SeoStub h1="Premarital compatibility is about conversations, not a percentage" />;
}
