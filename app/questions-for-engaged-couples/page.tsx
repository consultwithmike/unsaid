import { SeoStub } from "@/components/seo/SeoStub";

export const metadata = { title: "Questions for engaged couples — Unsaid" };

export default function Page() {
  return <SeoStub h1="You're engaged. What still isn't said?" />;
}
